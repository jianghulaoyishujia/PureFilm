完成功能：

    模型建立，主页面建立，详情页建立，主页到详情页链接建立


数据库重做后要重新建立映射表

绝对路径要用/static，不能用statics!!!!!!!!!!!!!!!!!!!!!!

映射表很重要,映射表映射的是电影和类型的多对多关系

json数据解析出来带有多余的部分

创建文件夹时前面不要带 /

urlretrieve第二个参数是文件名，不能有斜杠

难点： 在前端把不定数量的下载名字和下载链接对应上来

难点： Q查询 组合查询

难点：怎么点击图片发信息  解决

点击按钮或回车完成查询功能          done

点击输入框 value默认值消失   done

奇葩的错误啊，为毛year会==None，还不是is


slice 自定义过滤器


把<span>里的<a>标签设为和<div>一样高，就可以设置文字居中了

wsgi has no user  改middleware

记得设置宽度 100%

清除浏览器默认样式

    *{
    margin: 0;
    padding: 0;
}

设置层优先级：z-index 不让它透明

<div> 要在一行，把它们全取一样的class 再display:inline / inline-block


为什么主页完全固定，详情会跟着动？

footer不在最底部，设置上面内容为min-height自动调整

?type=3&country=china 带参数的URL不用专门设置路由分发

不能直接用前端传过来的参数去进行多对多查询

草  request.GET.get得到的参数前后都有引号，一定要去掉才可以用

第一次有效部署commit: beb8652